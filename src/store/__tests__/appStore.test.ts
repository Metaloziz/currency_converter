import appStore from 'store/appStore'

beforeEach(() => {})

describe('app store', () => {
  test('getRates', () => {
    // const { data, getRates } = appStore
    // expect(getRates()).toBe(data.rates)
  })
})
